"""
User models - menggunakan default Django User model
"""
from django.contrib.auth.models import User

# Kita menggunakan default Django User model
# Jika perlu custom fields, bisa extend dengan Profile model
